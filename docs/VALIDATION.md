# 验证状态

## 已确认

- Windows 11 x64，战网启动器更新后版本 2.52.12.17821，Blizzard 数字签名有效。
- 原配置为 CN，国服启动后能自动登录。
- 正常退出后，通过 TW 参数启动，登录地区字段变为 KR，日志访问国际站账号域名。
- 对应网络环境可用时，国际服账号登录成功。启动参数不承担网络加速，未隔离验证具体路由机制。
- 本地界面编译运行；真实战网与 Agent 已打开时，点击启动按钮被阻止，现有客户端没有被关闭。
- 隔离测试覆盖固定地区参数、路径处理、进程占用、检查失败、非法地区和重复启动等；没有调用真实客户端登录。

## 内容加载问题

曾观察到 `BLZBNTBNA000007D0 (2404)` 内容加载失败。此问题与账号登录失败分开记录。

账号服务、游戏服务和网页内容可能出现不同的连接结果。独立网页请求不带客户端账号会话，也不一定与客户端内嵌浏览器走相同网络路径，不能替代客户端实测。

待对照：普通游戏页面是否正常；错误页“在浏览器中打开”结果；加速器覆盖范围与网络组合。在证据不足前不修改系统网络、战网缓存或配置。

## 待验收

- 使用发布包完成国际服 → 国服 → 国际服往返，核对对应账号和目标游戏。
- Agent 退出等待及客户端更新场景。
- 国际服新闻和商城页面的独立网络问题。
- GitHub 首次云端编译（以 Actions 实际运行结果为准）。

代码编译通过不等于以上端到端验收完成。
